#include "main.h"
/**
 * _isupper - check if the letter is upper
 * @c: the number to be checked
 * Return: 1 if it is uppercase, 0 if not
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	return (0);
}
